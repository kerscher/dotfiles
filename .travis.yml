---
sudo: required
language: bash

services:
  - docker

before_install:
  - docker pull koalaman/shellcheck

script:
  - docker run -v "$(pwd):/opt" koalaman/shellcheck -x
    $(find . -type f -iname '*.sh' -print0 | xargs -0 -- printf '/opt/%s ')
