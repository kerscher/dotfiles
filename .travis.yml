# Based on https://github.com/koalaman/shellcheck/wiki/TravisCI

sudo: required

language: bash

services:
  - docker

before_install:
  - docker pull koalaman/shellcheck

script:
 - docker run -v $(pwd):/scripts koalaman/shellcheck -x /scripts/source/init.sh /scripts/source/text_editor.sh /scripts/source/keyboard-shortcuts.sh /scripts/source/directory-navigation.sh /scripts/source/prompt.sh
